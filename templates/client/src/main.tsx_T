import * as React from "react";
import * as ReactDom from "react-dom";

const root = document.createElement("div");
document.body.appendChild(root);

ReactDom.render(
  (<h1>
    Hattmo Template React App
  </h1>),
  root,
);
